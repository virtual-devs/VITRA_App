// ignore: file_names
import 'package:flutter/material.dart';

class HomeEmpresa extends StatelessWidget {
  const HomeEmpresa({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      child: Text('Arrendadora'),
    );
  }
}
